# 🏗️ 架构设计

## 设计灵感

本架构参考自：
1. [Auto-Dream](https://github.com/LeoYeAI/openclaw-auto-dream) — OpenClaw 做梦系统
2. **Claude Code 7 层记忆架构** — Anthropic 逆向工程分析

## Claude Code 7 层记忆架构（参考）

| Layer | 名称 | 成本 | 触发时机 |
|-------|------|------|----------|
| 1 | 工具结果存储 | 磁盘I/O | 每次工具调用 |
| 2 | 微压缩 | 零到极低 | 每轮对话前 |
| 3 | 会话记忆 | 1次 API 调用 | 对话中定期 |
| 4 | 完全压缩 | 1次完整 API 调用 | 上下文超阈值 |
| 5 | 自动记忆提取 | 1次 fork agent | 每次查询循环结束 |
| 6 | **做梦系统** | 1次 fork agent | 后台，累积足够后 |
| 7 | 跨 Agent 通信 | 不等 | Agent spawn/background |

### 关键设计原则

1. **分层防御，便宜的防止贵的触发**
2. **Prompt Cache 优化** — 一切为了 cache hit
3. **熔断器无处不在** — 3次失败就停止
4. **MEMORY.md 是索引，不是存储** — 每个条目一行 ~150 chars

## Beads 集成

[Beads](https://github.com/steveyegge/beads) 是 xiaoxi-dreams 的**任务追踪层**：

| Beads 命令 | 用途 |
|------------|------|
| `bd init` | 初始化工作区 |
| `bd create "任务" -p 0` | 创建 P0 任务 |
| `bd update <id> --claim` | 原子性领取任务 |
| `bd dep add <child> <parent>` | 建立依赖关系图 |
| `bd ready` | 列出无阻塞任务 |
| `bd show <id>` | 查看任务详情 |

**为什么用 Beads：**
- **持久化** — 任务不丢，跨会话
- **依赖图** — 自动推导执行顺序
- **Hash ID** — 多 Agent 不冲突
- **Dolt 驱动** — 版本控制，可回滚

## xiaoxi-Dreams 5 层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Dream Cycle (Cron)                      │
│                  每天 04:00 后台运行                        │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│   Collect     │    │  Consolidate  │    │   Evaluate    │
│   收集        │ →  │   整合        │ →  │   评估        │
├───────────────┤    ├───────────────┤    ├───────────────┤
│ 扫描7天日志   │    │ 去重/分配ID   │    │ 重要性评分   │
│ 提取决策/教训 │    │ 路由到对应层  │    │ 健康度计算   │
│ 识别任务/项目 │    │ 更新索引      │    │ 生成洞察     │
└───────────────┘    └───────────────┘    └───────────────┘
                                                      │
                                                      ▼
                                              ┌───────────────┐
                                              │    Report     │
                                              │    报告       │
                                              ├───────────────┤
                                              │ 推送给你     │
                                              │ 更新健康度   │
                                              │ 记录dream-log│
                                              └───────────────┘
```

## 记忆层级

### Layer 1: 工作记忆 (Work)
- **文件**: `memory/YYYY-MM-DD.md`
- **内容**: 每日原始日志
- **保留**: 7天后标记 consolidated

### Layer 2: 情景记忆 (Episodic)
- **文件**: `memory/episodes/*.md`
- **内容**: 项目叙事、事件时间线
- **触发**: 有重大事件时

### Layer 3: 长期记忆 (Long-term)
- **文件**: `MEMORY.md`
- **内容**: 事实、决策、人物、战略
- **上限**: 200行 / 25KB

### Layer 4: 程序记忆 (Procedural)
- **文件**: `memory/procedures.md`
- **内容**: 工作流、偏好、工具用法
- **更新**: 当工作方式改变时

### Layer 5: 索引记忆 (Index)
- **文件**: `memory/index.json`
- **内容**: 记忆元数据、健康度、上次做梦时间
- **用途**: 快速查询，不进入 context

## 健康度评分

```
health = (新鲜度×0.25 + 覆盖度×0.25 + 连贯度×0.2 + 效率×0.15 + 可达性×0.15) × 100
```

### 五维指标

| 指标 | 含义 | 权重 |
|------|------|------|
| 新鲜度 | 最近30天被引用的条目占比 | 0.25 |
| 覆盖度 | 最近14天有更新的知识类别 | 0.25 |
| 连贯度 | 至少有一个关联的条目占比 | 0.20 |
| 效率 | MEMORY.md 精简程度 | 0.15 |
| 可达性 | 记忆图谱连通程度 | 0.15 |

## 重要性评分

```
importance = (base_weight × recency_factor × reference_boost) / 8.0
```

### 标记权重

| 标记 | 权重 |
|------|------|
| `⚠️ PERMANENT` | 1.0（固定） |
| `🔥 HIGH` | 2.0 |
| `📌 PIN` | 1.5 |
| 普通条目 | 1.0 |

## 熔断器设计

- **连续失败 3 次** → 停止本次 Dream
- **60 分钟 stale lock** → 自动回收
- **无新日志** → 快速退出，节省 token
